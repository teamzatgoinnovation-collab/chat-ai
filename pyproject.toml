[project]
name = "chat_ai"
version = "0.2.0"
description = "Chat AI — ERPNext AI Platform"
authors = [{ name = "ZatGo Innovation" }]
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["chat_ai*"]
