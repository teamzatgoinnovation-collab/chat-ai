# Chat AI — ERPNext AI Platform

Generic, installable Frappe/ERPNext v16 AI platform.

```bash
bench get-app <this-repo>
bench --site <site> install-app chat_ai
bench --site <site> migrate
```

Configure **Chat AI Settings**, then use the Desk slide-out sidebar.

See [docs/](docs/) for architecture, SDK, and deployment.
